{
  "name": "@xxs3315/mbl-lib-example-data",
  "version": "1.0.0",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {
    "@xxs3315/mbl-typings": "workspace:*"
  },
  "devDependencies": {
    "copyfiles": "~2.4.1",
    "rimraf": "~6.0.0",
    "typescript": "~5.9.0"
  }
}
