{
  "name": "@xxs3315/mbl-core",
  "version": "1.0.0",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {
    "react-use-gesture": "^7"
  },
  "devDependencies": {
    "@types/react": "~19.1.0",
    "react": "~19.1.0",
    "react-dom": "~19.1.0",
    "rimraf": "~6.0.0"
  },
  "peerDependencies": {
    "react": "~19.1.0"
  }
}
